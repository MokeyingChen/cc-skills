CC StatusLine 安装包
===================

安装方法：

  在目标电脑上确保已安装 Node.js，然后运行：

      node install.js

  或者手动安装：

  1. 将 statusline.js 复制到 ~/.claude/statusline.js
  2. 编辑 ~/.claude/settings.json，添加 settings-patch.json 中的 statusLine 配置
     （注意将路径改为目标电脑上的绝对路径）

  安装后重启 Claude Code 即可生效。
